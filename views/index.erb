<% if @client %>
  Welcome, <%= @client %> | <a href='/logout'>Logout</a>
  <%= @files.inspect %>
  <ul>
    <% @files.each do |f| %>
      <li><a href="<%= f['path'] %>"><%= f['path'] %></a></li>
    <% end %>
  </ul>
<% else %>
  <a href='/login'>Login with Dropbox</a>
<% end %>